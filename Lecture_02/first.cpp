
#include<iostream>

using namespace std;

int main(){
    // int a,b;
    // cout<<"Enter first number: ";
    // cin>>a;
    // cout<<"Enter second number: ";
    // cin>>b;
    // cout<<"Your sum is: "<<a+b;


    // char c = 'a'+1;
    // cout<<c;

    // int c = 'a'+1;
    // cout<<c;


    // Arithmetic operator

    // + - * / %

    // int a = 20;
    // int b = 10;

    // cout<<a+b<<endl;
    // cout<<a-b<<endl;
    // cout<<a*b<<endl;
    // cout<<a/b<<endl;
    // cout<<a%b<<endl;    //% reminder , modulo operator

    // --------------------------------------

    // Assignment operator
    // a+=b;       //a=a+b
    // cout<<a;

    // --------------------------------------

    // Comparison operator
    // >,<,>=,<=,!=,==

    // cout<<(a>b)<<endl;
    // cout<<(a<b)<<endl;
    // cout<<(a>=b)<<endl;
    // cout<<(a<=b)<<endl;
    // cout<<(a==b)<<endl;
    // cout<<(a!=b)<<endl;


    // ---------------------------------------

    // left shift and right shift operator

    // int a = 20;

    // cout<<(a<<1)<<endl;   //20*2^1=40
    // cout<<(a<<2)<<endl;   //20*2^2=80

    // cout<<(a>>2)<<endl;   //20/2^2=5


    // ---------------------------------------

    // Logical operator
    // &&, ||, !

    // &&(and): both side should be true,then it will give true only otherwise false

    //  ||(or): both side should be false then it will give false only otherwise true

    // ! (not): it will invert the value true->false , false->true

    // cout<<(true && false)<<endl;
    // cout<<(true && 20)<<endl;   //ye number ko bhi true consider karta hai
    // cout<<(true && true)<<endl;
    // cout<<(true || false)<<endl;
    // cout<<(false || false)<<endl;
    // cout<<(!true)<<endl;



    // ---------------------------------------

    // Increment and Decrement operator
    // pre-increment and post-increment
    // int a = 10;
    // int b = a++;   //post-increment
    // post-increment pehle value assign karta hai fir increment karta hai

    // int b = ++a;   //pre-increment
    // pre-increment pehle increment karta hai fir value assign karta hai
    
    // cout<<b<<endl;   
    // cout<<a<<endl;   


    // --------------------

    // int a1 = 10;

    // int b1 = a1--;   //post-decrement
    // post-decrement pehle value assign karta hai fir decrement karta hai

    // int b1 = --a1;   //pre-decrement
    // pre-decrement pehle decrement karta hai fir value assign karta hai

    // cout<<b1<<endl;   
    // cout<<a1<<endl;


    // -------------------------------------

    // bitwise AND , OR , XOR

    // int a = 5;      // 0101
    // int b = 10;      // 1010

    // cout<<(a & b)<<endl;   // 0000 -> 0
    // cout<<(a | b)<<endl;   // 1111 -> 15
    // cout<<(0 ^ 0)<<endl;   // 0
    // cout<<(0 ^ 1)<<endl;   // 1
    // cout<<(1 ^ 1)<<endl;   // 0

    // XOR : if both bits are same then it will return 0 otherwise 1



    return 0;



}